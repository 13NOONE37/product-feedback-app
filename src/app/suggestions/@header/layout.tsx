import React, { FC, ReactNode } from 'react';

const Layout: FC<{
  categories: ReactNode;
  roadmapStatus: ReactNode;
}> = ({ categories, roadmapStatus }) => {
  return <header>header</header>;
};

export default Layout;
